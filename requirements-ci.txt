# Dependencies for CI unit + integration tests
# Heavy packages excluded:
# - pandas-ta: not available for Python 3.11+ on PyPI; indicators fall back to manual calc
# - shap/lightgbm: TechnicalAgent uses rule-based fallback when model not trained
# - sqlalchemy/alembic: not exercised in tests
# - hmmlearn/optuna: not exercised in tests
# Full production stack is validated in the Docker build job.

# Core framework
fastapi==0.110.0
pydantic==2.6.0
pydantic-settings==2.2.1
python-jose[cryptography]==3.3.0
slowapi==0.1.9

# Data
pandas==2.2.0
numpy==1.26.4

# Observability
structlog==24.1.0
prometheus-client==0.20.0

# HTTP — pin below 0.28 to avoid starlette TestClient breakage
httpx>=0.23.0,<0.28.0

# DB driver — needed by integration tests (api.main imports core.db.session)
# init_pool() is inside try/except so no real DB connection is required
asyncpg==0.29.0

# Redis (imported but mocked in unit tests)
redis==5.0.1

# Auth (needed for user_repository import chain)
passlib[bcrypt]==1.7.4

# Testing
pytest==8.0.2
pytest-asyncio==0.23.5
pytest-cov==4.1.0
hypothesis==6.98.0
