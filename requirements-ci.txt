# Minimal dependencies for CI unit tests
# Heavy packages (pandas-ta, shap, lightgbm, asyncpg, hmmlearn) are excluded:
# - pandas-ta: not available for Python 3.11+ on PyPI; indicators fall back to manual calc
# - shap/lightgbm: TechnicalAgent uses rule-based fallback when model not trained
# - asyncpg/sqlalchemy/alembic: no DB in unit tests
# - hmmlearn/optuna: not exercised in unit tests
# Full production stack is validated in the Docker build job.

# Core framework
fastapi==0.110.0
pydantic==2.6.0
pydantic-settings==2.2.1
python-jose[cryptography]==3.3.0
slowapi==0.1.9

# Data
pandas==2.2.0
numpy==1.26.4

# Observability
structlog==24.1.0
prometheus-client==0.20.0

# HTTP
httpx>=0.27.0

# Redis (imported but mocked in unit tests)
redis==5.0.1

# Testing
pytest==8.0.2
pytest-asyncio==0.23.5
pytest-cov==4.1.0
hypothesis==6.98.0
