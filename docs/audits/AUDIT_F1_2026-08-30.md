# AUDIT F1 — Habilitación mínima del core

> Auditoría de cierre de fase del `docs/PLAN_ELEVACION_MADUREZ_2026-08-28.md`.

- **Fase:** F1 — Habilitación mínima del core (entregables 1.1–1.9)
- **Fecha:** 2026-08-30
- **Commit auditado:** rama de trabajo sobre `043d218` (F0) — cambios F1 sin commitear al redactar
- **Auditor:** pase de verificación contra código + suite de tests
- **Alcance:** `scripts/run_pipeline.py`, `core/signals/`, `core/consensus/`, `core/strategies/`, `core/risk/risk_manager.py`, `core/adaptation/hmm_regime_detector.py`, `core/observability/decision_tracer.py`, `api/main.py`, `api/routes/{auth,trace}.py`, `core/config/{settings,constants}.py`, `core/ml/i1_gate_validator/`, `alembic/`, `docker/`, `tests/`, `.github/workflows/ci.yml`
- **Baseline:** `docs/audits/BASELINE_2026-08-28.md`

---

## Veredicto global

| Veredicto | |
|---|---|
| **GO CON CONDICIONES** | ✅ — 2 condiciones abiertas, ambas con fecha de cierre comprometida |

**Resumen.** Los 9 entregables de F1 están implementados y con tests verdes. El pipeline
se ejecuta de forma determinista, decide con los params validados por el gate I1
(fail-safe sin archivo aprobado), es trazable por `correlation_id` end-to-end, y la
escalada de privilegios trivial (F-01, P0) está cerrada. Suite `tests/unit + tests/quant`:
**390 passed / 2 skipped / 0 failed**. Condiciones abiertas:

1. **Pase de auditoría fresco** (Regla del plan: sesión nueva de Claude Code sin
   contexto, `TRADER_AI_PROMPT_MAESTRO.md` acotado a F1). No ejecutable en esta
   sesión. → antes de dar F1 por **GO** pleno.
2. **`pytest -m integration` en runner con Docker** — el job de CI está cableado
   (`services: postgres+redis`), el fixture `live_server` está probado localmente
   (5 tests del dashboard verdes), pero la corrida verde completa en CI está
   pendiente del primer push. Fallo residual conocido de **datos**
   (`test_all_symbols_have_1d_data`), no de infra → F2.4.

---

## Parte 1 — Checklist objetivo

### Hitos H1.1–H1.5

| Hito | Criterio | Evidencia | OK? |
|---|---|---|---|
| H1.1 | `docker compose up` levanta el stack; con `--workers 4` el pipeline corre 1×/símbolo/ventana | `docker compose config` OK (8 servicios: `db`,`migrate`,`redis`,`app`,`worker`,…); `test_api_state_has_no_scheduler_attr` (API sin APScheduler); `test_scheduler_single_owner*` (lock Redis, 1 dueño). Arranque real de contenedores no ejecutado aquí. | ✅ (estructural) |
| H1.2 | Con solo `XAUUSD.json`, el pipeline emite solo XAUUSD; resto `no_approved_strategy`; `grep default_v1` → 1 | `test_pipeline_only_trades_approved_symbols_repo_state` → `{"XAUUSD"}`; `grep -rn "default_v1" scripts core` → 1 (`signal_engine.py:87`); 6 legacy movidos a `i1_params/legacy/` | ✅ |
| H1.3 | `GET /trace/{id}` reconstruye un ciclo completo (mocks) | `test_pipeline_cycle_produces_full_trace` (8 etapas + `correlation_id` en la señal persistida); `test_trace_endpoint` (200 / 404) | ✅ |
| H1.4 | `pytest tests/unit tests/quant` → 0 failed; `pytest -m integration` (Docker) → 0 failed/errors | unit+quant: **390 passed / 2 skipped / 0 failed**. `-m integration`: pendiente runner Docker (condición 2) | ⚠️ parcial |
| H1.5 | No hay camino a `admin` sin un `admin` previo | `test_public_register_cannot_set_role` (body `role:admin` → usuario `viewer`); `test_admin_endpoint_requires_admin` (401/403/201) | ✅ |

### Tests nominales del checklist F1

| Test | Estado |
|---|---|
| `test_scheduler_single_owner` (+ `_concurrent`) | ✅ |
| `test_startup_live_without_db_exits` | ✅ |
| `test_risk_portfolio_exposure_aggregation` | ✅ |
| `test_pipeline_only_trades_approved_symbols` (+ `_generalises`) | ✅ |
| `test_universe_is_single_source` | ✅ |
| `test_pipeline_cycle_produces_full_trace` | ✅ |
| `test_public_register_cannot_set_role` | ✅ |

Comando: `pytest -q tests/unit -k "<los 7>"` → **9 passed** (2 nombres cubren 2 casos).

### Verificación por entregable

| # | Entregable | Evidencia | OK? |
|---|---|---|---|
| 1.1 | Split worker/API + lock Redis | `core/infrastructure/scheduler_lock.py`; API `lifespan` sin scheduler/`_refresh_task`; `docker-compose` servicio `worker`; `pipeline_cycles_total{symbol}` | ✅ |
| 1.2 | `alembic/` en imagen + fail-fast live + `/health.db_initialized` + HEALTHCHECK stdlib | `docker/Dockerfile` (`COPY alembic/` + `urllib`); `api/main.py` (`raise` si `live`); `test_startup_db_policy.py` (3 ✅) | ✅ |
| 1.3 | `TRADED_UNIVERSE` fuente única + `BROKER_SYMBOL_MAP` + skip sin datos | `core/config/constants.py`; `settings`/`SCHEDULE`/`PIPELINE_SYMBOLS` derivan; `test_universe_single_source.py` (13 ✅); `SPX500`→`US500` | ✅ |
| 1.4 | `StrategyRegistry` catálogo único + params I1 al pipeline + fail-safe | `core/strategies/approved_params.py`; `strategy_registry.strategy_ids()`; Gate 0b `no_approved_strategy` + confirmación `signal_vetoed_by_strategy`; gate escribe `<SYMBOL>.json`; `test_approved_strategy_wiring.py` (7 ✅) | ✅ |
| 1.5 | Fix HMM + exposición agregada + `recent_trades` reales | `hmm_regime_detector.py` (drop NaN, `MIN_TRAIN_SAMPLES`, `diag`); `risk_manager.aggregate_open_exposure` + pérdida diaria directa; `portfolio_manager.get_recent_trades`; `test_hmm_regime_detector.py` 12 ✅ (antes 2 ❌ — **D-12 cerrado**) | ✅ |
| 1.6 | `correlation_id` + `GET /trace/{id}` + productor (repo + AuditLog) | `decision_tracer.TraceStore`; `correlation_id` en modelos; `run_pipeline` registra 8 etapas + `AuditLog` por decisión; `api/routes/trace.py`; `test_pipeline_trace.py` (3 ✅). **Diferido:** columna DB `correlation_id` (migración alembic → F3.2) | ✅ (con nota) |
| 1.7 | `REGISTRATION_ENABLED` + registro solo `viewer` + `POST /auth/users` admin + validador JWT siempre activo | `core/config/settings.py`; `api/routes/auth.py`; `test_security_floor.py` (6 ✅). **F-01 (P0) y F-08 cerrados** | ✅ |
| 1.8 | Fixtures `docker_services`/`live_server` + marca `integration` + `bandita`→`bandit` + job CI | `tests/conftest.py`; `tests/integration/conftest.py`; `pytest.ini` (`-m "not integration"`); `.github/workflows/ci.yml`; `test_hermetic_test_infra.py` (5 ✅). **G-8, D-10, F-07 cerrados** | ✅ |
| 1.9 | `docker compose up` reproducible + migraciones + runbook | `alembic/env.py` async + `DATABASE_URL` de env (sin psycopg2); servicio one-shot `migrate`; `docs/RUN_CORE_VALIDATION.md`; `test_reproducible_env.py` (6 ✅); `alembic upgrade head --sql` genera las 7 migraciones | ✅ |

---

## Parte 2 — Pase de auditoría fresco

**Estado: PENDIENTE.** Requiere sesión nueva de Claude Code sin contexto de la
implementación, aplicando `TRADER_AI_PROMPT_MAESTRO.md` acotado a
`scripts/run_pipeline.py`, `core/signals/`, `core/consensus/`, `core/strategies/`,
`core/risk/risk_manager.py`, `docker/`. Preguntas guía: ¿el pipeline es
determinista? ¿decide con lo que se valida? ¿alguna etapa produce basura silenciosa?

---

## Parte 3 — No-regresión (8 hallazgos "Resuelto" de 2026-08)

| Hallazgo previo | Estado esperado | Estado actual | Evidencia |
|---|---|---|---|
| Kill switch fail-open | Resuelto | Resuelto | `kill_switch_redis.py:85-96` intacto; ADR-005 lo ratifica; `test_kill_switch.py` verde |
| Gate I1 ignora holdout (criterio) | Resuelto | Resuelto | `gate_validator.py` criterio con holdout intacto; `tests/quant` 14 ✅ |
| WebSocket sin auth | Resuelto | Resuelto | `api/routes/websocket.py` sin cambios; `test_websocket_auth.py` verde |
| IDOR en órdenes | Resuelto | Resuelto | `api/routes/execution.py` `_owns_order` sin cambios; `test_execution_idor.py` verde |
| DB nunca inicializada / scheduler comentado | Resuelto | Resuelto y **endurecido** | `api/main.py` `init_pool`+`run_migrations` + fail-fast en `live` (F1.2) |
| `POST /execution` type mismatch | Resuelto | Resuelto | `risk_manager.validate_signal` acepta `Signal\|dict`; tests de ejecución verdes |
| `docker-compose.yml` YAML inválido | Resuelto | Resuelto | `docker compose config` OK (8 servicios) |
| Dashboard: 3 implementaciones + god file | Resuelto | Resuelto | `docs/archive/app_streamlit_legacy/` intacto; SPA única |

**Sin regresiones.** F0 (ADRs, baseline) intacto.

---

## Score de madurez (estimación vs baseline)

| Área | Baseline | Post-F1 (est.) | Δ | Motivo |
|---|---|---|---|---|
| Core Trading | 5.0 | 5.5 | +0.5 | determinista, decide con params validados (salto real en F2) |
| Estrategias | 3.0 | 4.5 | +1.5 | fuente única + params I1 cableados + fail-safe |
| Risk | 5.5 | 6.5 | +1.0 | exposición agregada de cartera + pérdida diaria directa |
| Seguridad | 3.0 | 5.5 | +2.5 | F-01 (P0) cerrado, validador JWT siempre activo |
| Observabilidad | 4.5 | 5.5 | +1.0 | productor de traza + `GET /trace/{id}` + `AuditLog` del pipeline |
| Testing | 5.5 | 6.5 | +1.0 | integración hermética, e2e ejecutable, marca `integration` |
| Performance | 4.5 | 5.5 | +1.0 | fin del 4× por workers |
| DevSecOps | 4.0 | 5.0 | +1.0 | CI `lint` reparado, job de integración con servicios |
| Multi-Asset | 4.0 | 4.5 | +0.5 | universo único canónico |
| IA/ML | 3.5 | 4.0 | +0.5 | HMM ya no revienta (D-12) |

**Score global estimado: 4.6 → ~5.4.**

---

## Condiciones de cierre

- [x] El pipeline se ejecuta de forma determinista (gates + lock + universo único).
- [x] Decide con los params validados por I1; sin archivo aprobado ⇒ no opera.
- [x] Trazable end-to-end por `correlation_id`.
- [x] Sin escalada de privilegios trivial (F-01 cerrado).
- [x] `pytest tests/unit tests/quant` → 0 failed.
- [ ] **Condición 1:** pase de auditoría fresco.
- [ ] **Condición 2:** `pytest -m integration` verde en runner con Docker (CI).

**F1 = GO CON CONDICIONES.** La Fase 2 puede arrancar en paralelo al cierre de las
dos condiciones.
